fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
numpy==2.2.1
matplotlib==3.10.0
qdrant-client==1.12.2

# Optional (enable real Gemma-family embeddings). The app runs without these
# via a lightweight fallback vectorizer; install to use EmbeddingGemma.
# sentence-transformers==3.3.1
# torch
